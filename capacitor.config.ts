import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.regil.tarea',
  appName: 'tarea_6_20212252',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
